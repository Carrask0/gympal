import { SessionCard } from '../components/SessionCard/SessionCard';

function SessionPage({ editting }) {
    return (
       <SessionCard editting={editting} />
    );
};

export default SessionPage;